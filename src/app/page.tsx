import Hero from "@/components/Hero";
import RiveToolWrapper from "@/components/RiveToolWrapper";

export default function Home() {
  return (
    <main className="min-h-screen bg-rive-bg">
      <Hero />
      <RiveToolWrapper />
    </main>
  );
}
